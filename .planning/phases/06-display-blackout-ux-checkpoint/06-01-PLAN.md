---
phase: 06-display-blackout-ux-checkpoint
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - PROJECT.md
  - README.md
  - e2e/tests/weston-warning.spec.ts
  - e2e/compose.test.yml
autonomous: true
requirements:
  - UX-01
  - UX-02
must_haves:
  truths:
    - "PROJECT.md Key Decisions table gains a new row recording UX-01 → option (a) with the canonical rationale paragraph"
    - "README.md exists at repo root and contains a 'Before you click Update on flutter or weston' section naming the 5-30s blackout window, the Rollback safety net, and the pre-action toast confirmation"
    - "e2e/tests/weston-warning.spec.ts is a RED-first Playwright spec that asserts the pre-action toast fires for a service whose name contains 'weston', includes the words 'display' and 'flicker' (case-insensitive), and gates the recreate behind operator confirmation"
    - "e2e/compose.test.yml has a weston-stub service block with hmi-update.watch=true if Phase 5 did not already add one (idempotent — verify-before-add)"
    - "No production code changes ship in this plan — UX-03 (option (b) deliverables) is explicitly NOT shipped because option (a) was chosen"
    - "make e2e exits 0 with the new spec included (verifies UI-08 contract from Phase 5)"
  artifacts:
    - path: "PROJECT.md"
      provides: "UX-01 decision record + rationale paragraph in Key Decisions table"
      contains: "UX-01"
    - path: "README.md"
      provides: "Operator-facing README with the load-bearing display-blackout callout (UX-02)"
      contains: "Before you click Update on flutter"
    - path: "e2e/tests/weston-warning.spec.ts"
      provides: "Playwright e2e verification that the Phase 5 pre-action toast fires for weston substring matches"
      contains: "test('shows display flicker warning"
    - path: "e2e/compose.test.yml"
      provides: "weston-stub service for the warning spec to target (idempotent add)"
      contains: "weston-stub"
  key_links:
    - from: "e2e/tests/weston-warning.spec.ts"
      to: "Phase 5 UI-08 pre-action toast implementation"
      via: "page.getByRole('alertdialog') / toast selector established by Phase 5"
      pattern: "weston-warning"
    - from: "PROJECT.md Key Decisions::UX-01"
      to: ".planning/phases/06-display-blackout-ux-checkpoint/06-CONTEXT.md"
      via: "decision rationale verbatim from CONTEXT.md decisions section"
      pattern: "UX-01"
    - from: "README.md::Before you click Update"
      to: ".planning/research/PITFALLS.md Pitfall 5"
      via: "paraphrase + link"
      pattern: "Pitfall 5"
---

<objective>
Land Phase 6 — Display-Blackout UX Checkpoint — under the locked decision option (a):

1. **PROJECT.md** — append a new row to the Key Decisions table recording UX-01 → option (a), with the canonical 1-paragraph rationale (verbatim from 06-CONTEXT.md `<decisions>` section).
2. **README.md (NEW)** — create the operator-facing repo README at `/Users/jonb/Projects/tmp/README.md`. The file does not exist today. Required content: a one-paragraph intro, quick-start pointers, and the load-bearing "Before you click Update on flutter or weston" section that names the 5-30s blackout window, the Phase-5 pre-action toast, and the Rollback safety net.
3. **e2e/tests/weston-warning.spec.ts (NEW)** — RED-first Playwright spec asserting the pre-action toast fires for a service with `weston` in its name. Verifies UI-08 (Phase 5) actually triggers on the substring; closes the UX-02 → UI-08 trace.
4. **e2e/compose.test.yml** — ensure a `weston-stub` service exists for the spec to target. Idempotent: if Phase 5 already added it, this plan verifies; if not, this plan adds the minimal service block.

Purpose: UX-01 / UX-02 are documentation-led product decisions. Phase 5 already shipped UI-08 (the pre-action toast). This plan closes the documentation surface (PROJECT.md decision row + README callout) and pins the UI-08 contract in CI via the verification spec.

UX-03 (option (b) two-step prepare/switch UX) is **NOT shipped** — option (a) was chosen, so the state schema, third per-row button, and Stage 1 / Stage 2 e2e spec do not land.

This plan is a single wave with no dependencies on other Phase 6 plans (there are no other Phase 6 plans). Plan 06-01 depends only on Phase 5 having completed UI-08.

Output: One new file (README.md), one extended existing file (PROJECT.md), one new spec (weston-warning.spec.ts), one possibly-extended compose override (compose.test.yml). `make e2e` green; manual smoke confirms toast in a real browser.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-display-blackout-ux-checkpoint/06-CONTEXT.md
@.planning/phases/06-display-blackout-ux-checkpoint/06-RESEARCH.md
@.planning/phases/05-web-ui-completeness/05-CONTEXT.md
@.planning/research/PITFALLS.md

<interfaces>
From Phase 5 (assumed shipped under 05-CONTEXT.md):
- UI-08 — case-insensitive substring match on compose service name for `flutter` or `weston` triggers a pre-action "display may flicker" confirmation toast.
- The toast's confirm/cancel mechanism is owned by Phase 5; the spec asserts only that a dialog/toast surfaces with the load-bearing keywords ("display", "flicker") before the recreate fires.

From e2e/compose.test.yml (Phase 1+2+3+4+5):
- Existing services (timescaledb-stub, hmi-update, zot, etc.) — `weston-stub` either already exists (added by Phase 5 alongside UI-08) or needs adding here.
- Service block shape: `image: alpine:latest`, `command: ["sleep", "infinity"]`, `labels: {hmi-update.watch=true}` — minimal stub that the discovery loop enumerates.

From e2e/tests/ (Phase 1 onward):
- Playwright fixtures: `globalSetup` brings up the compose stack, `globalTeardown` tears down. Tests target `http://localhost:8080`.
- Spec pattern from concurrent-actions.spec.ts (Phase 4) and Phase 5's UI specs — same shape: navigate, click button, await dialog/toast, assert content.

From .planning/research/PITFALLS.md Pitfall 5:
- 5-30s blackout window for `flutter` / `weston` recreate.
- Recreating `weston` cascades to all Wayland clients.
- Rollback is the safety net.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PROJECT.md Key Decisions row for UX-01 + README.md operator-facing callout</name>
  <files>PROJECT.md, README.md</files>
  <read_first>
    - PROJECT.md (current — locate the Key Decisions table; append-only, do NOT remove or modify existing rows)
    - .planning/phases/06-display-blackout-ux-checkpoint/06-CONTEXT.md (lines 30-60 — canonical UX-01 decision rationale; copy the 1-paragraph block verbatim)
    - .planning/research/PITFALLS.md (lines 126-147 — Pitfall 5 source material for the README paraphrase)
    - .planning/phases/05-web-ui-completeness/05-CONTEXT.md (Phase 5 pre-action toast UI-08 — the README references this UX surface)
    - .planning/PROJECT.md "Container labels reference" section (Phase 4 — README briefly references the labels operators need to know)
  </read_first>
  <behavior>
    PROJECT.md edit:
    - Locate the existing `## Key Decisions` table (Phase 1+).
    - Append ONE new row to the table with:
      - Decision: "UX-01 — display-blackout UX for flutter/weston: chose option (a) (README warning + Phase-5 pre-action toast)"
      - Rationale: the canonical 1-paragraph rationale from 06-CONTEXT.md verbatim (collapsed to fit a table cell — use a brief 2-sentence version in the cell and link to README + Phase 6 CONTEXT for full text), e.g.: "Phase 5 already ships a pre-action 'display may flicker' confirmation toast (UI-08); Rollback is the safety net. Options (b) and (c) double the surface area (new schema field + endpoint + button; or per-service label discipline); option (a) preserves the brief's 'one button per container' Core Value. See README.md and .planning/phases/06-.../06-CONTEXT.md for the full rationale."
      - Outcome: "Locked — no Phase 6 code changes; README callout + Playwright verification spec ship instead"
    - Do NOT remove any existing rows. Do NOT renumber. The table grows by exactly one row.

    README.md create:
    - File does NOT exist today at /Users/jonb/Projects/tmp/README.md. This task creates it.
    - Operator-facing tone — brief, clear, no marketing.
    - Sections (in order):
      1. Title: `# hmi-update`
      2. One-paragraph intro (paraphrase PROJECT.md "What This Is").
      3. `## Quick start` — `docker compose up -d hmi-update` + a brief note that the `id -g docker` install step is documented in Phase 7's deployment runbook (forward reference; this README is the seed Phase 7 extends).
      4. `## Before you click Update on flutter or weston` — the LOAD-BEARING UX-02 section. Required content:
         - Names the 5-30s blackout window explicitly ("Recreating the flutter or weston container will blank the operator display for 5-30 seconds while the new container starts and reaches first paint.").
         - Notes that recreating `weston` is worse than recreating `flutter` (cascade — all Wayland clients restart).
         - States that the UI will surface a pre-action "display may flicker" confirmation toast (Phase 5 UI-08) before the recreate fires; the operator can cancel.
         - Points at the Rollback button as the safety net if the blackout is unwanted ("Rollback returns the container to the previous digest in <15 seconds, entirely from the local image cache — works even with no network.").
         - Brief mention of Force Pull as the recovery path if the local image was accidentally removed.
         - One-line pointer: "Full failure-mode analysis: .planning/research/PITFALLS.md Pitfall 5."
      5. `## Container labels` — a brief table mirroring PROJECT.md "Container labels reference" (operators need to know `hmi-update.watch`, `allow-update`, `allow-rollback`, `wait-for-healthy`, `tag-pattern`).
      6. `## Project pointers` — links to PROJECT.md, the planning ROADMAP, the API surface (forward reference to Phase 4's API.md).
    - The README is intentionally a seed — Phase 7 extends it with the full install runbook (DEPLOY-08 / Pitfall 9). This phase ships only the UX-02 callout and the minimum surrounding context.
  </behavior>
  <action>
    (a) Edit PROJECT.md — append one row to the Key Decisions table

    Locate the existing Key Decisions table (currently ends around line 110 with the "TDD: Playwright e2e tests written before implementation" row). Append a new row using the table's existing 3-column format (Decision | Rationale | Outcome). Use the Edit tool with `old_string` set to the last existing table row and `new_string` set to that row + the new UX-01 row, preserving alignment.

    The new row content:

    | Decision | Rationale | Outcome |
    |----------|-----------|---------|
    | **UX-01 — display-blackout UX for flutter/weston: chose option (a) (README warning + Phase-5 pre-action toast)** | Phase 5 already ships a pre-action "display may flicker" confirmation toast (UI-08) for service names matching `flutter` / `weston`; Rollback is the safety net. Options (b) two-step prepare/switch and (c) per-service danger flag both double the surface area (new schema field + endpoint + third button; or per-service label discipline). Option (a) preserves the brief's "one button per container" Core Value with zero Phase-6 code changes. Full rationale: `.planning/phases/06-display-blackout-ux-checkpoint/06-CONTEXT.md` and README.md "Before you click Update on flutter or weston". | Locked — Phase 6 ships documentation only; UX-03 (option (b) deliverables) explicitly not shipped |

    (b) Create README.md at repo root — full content shape:

    ```markdown
    # hmi-update

    A single Go container that detects when `:latest` Docker images have been re-pushed for the containers running on Centroid's elevator HMI boxes, and gives Centroid field engineers per-container **Update** and **Rollback** buttons via a small Svelte web UI on the HMI LAN. Replaces a fragile patched WUD 8.2.2 setup and a heavier Komodo-based alternative with a tool that has rollback built in, ships as one image, and persists everything in a single JSON file alongside the compose stack.

    Web UI: `http://<hmi>:8080/`

    ## Quick start

    Drop the `hmi-update` service block into your existing `docker-compose.yml` and run:

    ```sh
    docker compose up -d hmi-update
    ```

    The full install runbook — including the `id -g docker` step required for the distroless nonroot user to reach the docker socket — is documented in the Phase 7 deployment runbook. See `.planning/PROJECT.md` "Installation prerequisites".

    ## Before you click Update on flutter or weston

    The `flutter` and `weston` containers draw the operator's elevator display. Recreating either of them blanks the screen for 5-30 seconds while the new container starts and reaches first paint. The exact duration depends on whether the new image's layers are already extracted locally (faster) or need a cold pull (slower), and on the application's own cold-start time (a Flutter app typically takes 2-10s to draw its first frame on HMI hardware).

    **Recreating `weston` is worse than recreating `flutter`** — `weston` is the Wayland compositor, and tearing it down disconnects every Wayland client (`flutter` and any others), so all of them restart together.

    The web UI will show a **"display may flicker" confirmation toast** before the recreate fires when the targeted service name contains `flutter` or `weston` (case-insensitive substring match). The operator can cancel from the toast — nothing happens until the toast is confirmed.

    If the operator confirms and the blackout is unwanted, **Rollback returns the container to the previous digest in under 15 seconds, entirely from the local image cache** — it works even with no network connection to the registry. Rollback is the safety net for "I clicked Update and got a blackout I didn't want."

    If the new image's local cache was accidentally removed (a `docker image prune` mishap), **Force Pull** re-pulls the `:latest` image without recreating the container — it's the recovery path if Update fails to find the local image.

    Full failure-mode analysis: `.planning/research/PITFALLS.md` Pitfall 5.

    ## Container labels

    | Label | Purpose | Default if absent |
    |-------|---------|-------------------|
    | `hmi-update.watch=true` | Mark a container as watched | Not watched |
    | `hmi-update.tag-pattern=<regex>` | Constrain upstream tag candidacy (e.g. `^latest-pg17$` on timescaledb) | Any tag matches |
    | `hmi-update.allow-update=false` | Server refuses Update for this container (SAFE-01) | Update allowed |
    | `hmi-update.allow-rollback=false` | Server refuses Rollback for this container | Rollback allowed |
    | `hmi-update.wait-for-healthy=true` | Extend verify-after-recreate to wait for `State.Health.Status == "healthy"` (60s window) | 15s consecutive-Running window |

    See `.planning/PROJECT.md` "Container labels reference" for the canonical table.

    ## Project pointers

    - **Full requirements + decisions:** `.planning/PROJECT.md`
    - **Roadmap + phase plans:** `.planning/ROADMAP.md`
    - **HTTP API:** `API.md` (Phase 4 — `/api/state`, `/api/containers/{service}/update`, `/rollback`, `/force-pull`)
    - **Research (pitfalls, registry mechanics, distroless GID, atomic writes):** `.planning/research/`
    ```

    Write this content to README.md verbatim.
  </action>
  <verify>
    <automated>cd /Users/jonb/Projects/tmp && grep -F 'UX-01' PROJECT.md && grep -F 'option (a)' PROJECT.md && test -f README.md && grep -F 'Before you click Update on flutter' README.md && grep -F '5-30 seconds' README.md && grep -F 'Rollback' README.md && grep -F 'Force Pull' README.md && grep -F 'Pitfall 5' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'UX-01' PROJECT.md` returns at least 1 match (Key Decisions row)
    - `grep -F 'option (a)' PROJECT.md` returns at least 1 match
    - `grep -F 'one button per container' PROJECT.md` returns at least 1 match (rationale)
    - `test -f README.md` succeeds (README.md created at repo root)
    - `grep -F 'Before you click Update on flutter' README.md` returns at least 1 match (load-bearing section)
    - `grep -F '5-30 seconds' README.md` returns at least 1 match (blackout window named)
    - `grep -F 'weston' README.md` returns at least 2 matches (section heading + cascade note)
    - `grep -F 'display may flicker' README.md` returns at least 1 match (toast wording reference)
    - `grep -F 'Rollback' README.md` returns at least 1 match (safety net mention)
    - `grep -F 'Force Pull' README.md` returns at least 1 match (recovery path mention)
    - `grep -F 'Pitfall 5' README.md` returns at least 1 match (deep-link to PITFALLS.md)
    - `grep -F 'docker compose up -d hmi-update' README.md` returns at least 1 match (quick start)
    - PROJECT.md line count is greater than before (append-only verification — no existing rows removed)
  </acceptance_criteria>
  <done>UX-01 decision is recorded in PROJECT.md with the canonical rationale; README.md exists at repo root with the load-bearing "Before you click Update on flutter or weston" callout; both deliverables verifiable by grep.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: e2e/tests/weston-warning.spec.ts RED-first Playwright spec + e2e/compose.test.yml weston-stub service (idempotent)</name>
  <files>e2e/tests/weston-warning.spec.ts, e2e/compose.test.yml</files>
  <read_first>
    - e2e/compose.test.yml (current state — Phase 5 may have already added a weston-stub service; verify before adding)
    - e2e/tests/ (browse for the closest cousin spec — likely `concurrent-actions.spec.ts` from Phase 4 or a UI-08 spec from Phase 5; mirror the structure)
    - .planning/phases/05-web-ui-completeness/05-CONTEXT.md (Phase 5 UI-08 trigger mechanism — substring match on service name; toast selector pattern)
    - .planning/phases/06-display-blackout-ux-checkpoint/06-CONTEXT.md (decisions section — spec asserts on keywords "display" and "flicker", not exact wording)
  </read_first>
  <behavior>
    e2e/tests/weston-warning.spec.ts:
    - Standard Playwright spec structure (per the project's e2e conventions).
    - Two test cases:
      1. `test('shows display flicker warning before Update on weston-stub', async ({ page }) => { ... })` — happy path:
         - Navigate to `http://hmi-update:8080/` (or the e2e fixture URL — match what other specs use).
         - Wait for the table to populate (Phase 5 UI).
         - Locate the row whose service name contains `weston` (case-insensitive).
         - Click the row's Update button.
         - Assert a dialog / toast appears containing the words "display" AND "flicker" (case-insensitive). Use a flexible selector — `page.getByRole('alertdialog')` OR `page.getByRole('dialog')` OR a toast-component selector that Phase 5 ships. If Phase 5's selector is unknown at planning time, use `page.locator('[role=alertdialog], [role=dialog], .toast').filter({ hasText: /display.*flicker|flicker.*display/i })`.
         - Assert the toast has a "Cancel" / "Dismiss" affordance.
      2. `test('cancel from warning toast does not trigger recreate on weston-stub', async ({ page }) => { ... })` — cancel path:
         - Same setup as above.
         - Click Cancel on the toast.
         - Assert the toast dismisses.
         - Assert no POST to `/api/containers/weston-stub/update` fired (intercept via `page.route` or check that the container's `ActionInFlight` field remains empty in `/api/state`).
    - Spec is RED-first: if Phase 5 UI-08 is missing or the substring detection is wrong, the test fails. This is the verification that locks the Phase 5 → Phase 6 contract.
    - Test file uses Playwright's `test.describe` to group the two cases under "weston pre-action warning toast (UX-02 → UI-08 contract)".

    e2e/compose.test.yml:
    - Idempotent verify-then-add: read the current file; if a service named `weston-stub` (or any service block whose `container_name` / service key contains `weston`) already exists, the file is unchanged. If absent, add the minimal block:
      ```yaml
      weston-stub:
        image: alpine:latest
        container_name: weston-stub
        command: ["sleep", "infinity"]
        labels:
          - "hmi-update.watch=true"
        # Display-drawing service stub for Phase 6 UX-02 verification
        # (the spec checks that the UI's pre-action toast fires on
        # substring match against "weston" in the service name).
        restart: unless-stopped
      ```
    - Place the service block near other test stubs (e.g., next to `timescaledb-stub` if it exists).
  </behavior>
  <action>
    (a) Inspect e2e/compose.test.yml — search for a `weston` service block. If found, no edit. If absent, add the service block above near the other stub services.

    (b) Inspect e2e/tests/ for the canonical pre-action-toast selector pattern from Phase 5 (likely in a spec file with "warning", "toast", or "weston" in the name). Use the same selector if found; otherwise use the flexible role/text fallback documented in `<behavior>` above.

    (c) Create e2e/tests/weston-warning.spec.ts with the two test cases above. Spec body shape:

    ```typescript
    import { test, expect } from '@playwright/test';

    test.describe('weston pre-action warning toast (UX-02 → UI-08 contract)', () => {
      test.beforeEach(async ({ page }) => {
        await page.goto('/');
        // Wait for the container table to populate; Phase 5 UI lists services.
        await expect(page.getByRole('row').filter({ hasText: /weston/i })).toBeVisible({ timeout: 10_000 });
      });

      test('shows display flicker warning before Update on weston-stub', async ({ page }) => {
        const westonRow = page.getByRole('row').filter({ hasText: /weston/i }).first();
        await westonRow.getByRole('button', { name: /update/i }).click();

        // The pre-action toast (UI-08) MUST surface for weston substring matches.
        // Spec asserts only the load-bearing keywords; exact copy is owned by Phase 5.
        const warning = page.locator('[role=alertdialog], [role=dialog], .toast')
          .filter({ hasText: /display.*flicker|flicker.*display/i });
        await expect(warning).toBeVisible({ timeout: 5_000 });
        await expect(warning).toContainText(/display/i);
        await expect(warning).toContainText(/flicker/i);

        // Operator must have a cancel affordance.
        await expect(warning.getByRole('button', { name: /cancel|dismiss|no/i })).toBeVisible();
      });

      test('cancel from warning toast does not trigger recreate on weston-stub', async ({ page }) => {
        let updatePostFired = false;
        await page.route('**/api/containers/*/update', (route) => {
          updatePostFired = true;
          route.continue();
        });

        const westonRow = page.getByRole('row').filter({ hasText: /weston/i }).first();
        await westonRow.getByRole('button', { name: /update/i }).click();

        const warning = page.locator('[role=alertdialog], [role=dialog], .toast')
          .filter({ hasText: /display.*flicker|flicker.*display/i });
        await expect(warning).toBeVisible({ timeout: 5_000 });

        await warning.getByRole('button', { name: /cancel|dismiss|no/i }).click();
        await expect(warning).toBeHidden({ timeout: 2_000 });

        // No update POST should have fired — cancel gates the action server-side too.
        // Small delay to catch any racing POST.
        await page.waitForTimeout(500);
        expect(updatePostFired).toBe(false);
      });
    });
    ```

    Adjust selectors if Phase 5 establishes a more specific contract (e.g., a `data-testid="weston-warning-toast"`); the flexible fallback above works whether Phase 5 uses native `<dialog>`, ARIA roles, or a custom toast component.
  </action>
  <verify>
    <automated>cd /Users/jonb/Projects/tmp && grep -F 'weston-stub' e2e/compose.test.yml && test -f e2e/tests/weston-warning.spec.ts && grep -F 'shows display flicker warning' e2e/tests/weston-warning.spec.ts && grep -F 'cancel from warning toast' e2e/tests/weston-warning.spec.ts && grep -F 'flicker' e2e/tests/weston-warning.spec.ts && make e2e</automated>
  </verify>
  <acceptance_criteria>
    - `test -f e2e/tests/weston-warning.spec.ts` succeeds
    - `grep -F 'shows display flicker warning' e2e/tests/weston-warning.spec.ts` returns at least 1 match
    - `grep -F 'cancel from warning toast' e2e/tests/weston-warning.spec.ts` returns at least 1 match
    - `grep -F 'UX-02' e2e/tests/weston-warning.spec.ts` returns at least 1 match (or `UI-08` — the describe block traces the contract)
    - `grep -F 'display' e2e/tests/weston-warning.spec.ts` returns at least 2 matches (selector + assertion)
    - `grep -F 'flicker' e2e/tests/weston-warning.spec.ts` returns at least 2 matches
    - `grep -F 'weston-stub' e2e/compose.test.yml` returns at least 1 match (service exists)
    - `grep -F 'hmi-update.watch=true' e2e/compose.test.yml` matches the weston-stub block (the discovery loop enumerates it)
    - `make e2e` exits 0 (the full Playwright suite including the new spec passes)
    - The spec runs in CI and is included in the default Playwright config (`playwright.config.ts` testDir picks it up — no config change required if it follows the existing convention)
  </acceptance_criteria>
  <done>Playwright e2e spec pins the Phase 5 UI-08 contract for weston substring matches; weston-stub service exists in e2e/compose.test.yml; `make e2e` is green. The spec is the verify→green loop for UX-02; UX-01 is verified by the PROJECT.md grep in Task 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → web UI | The operator triggers Update via the browser; the pre-action toast is the only gate between click and recreate. UX-02 documentation surface explains this trust contract. |
| Phase 5 (UI-08) → Phase 6 (verification) | Phase 6's Playwright spec is the contract test that pins Phase 5's substring detection. A future Phase 5 refactor that changes the trigger mechanism MUST update this spec. |
| README operator-facing docs → operator's mental model | The README is the only operator-facing surface (PROJECT.md and .planning/ are contributor-facing). The accuracy of the README callout is the load-bearing trust boundary for UX-02. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | I (Info Disclosure / mismatched docs) | README claims a UX behavior Phase 5 did not actually ship | mitigate | Task 2's Playwright spec verifies the toast actually surfaces with the load-bearing keywords; CI fails if Phase 5's UI-08 regressed or never shipped. The spec is the empirical contract test. |
| T-06-01-02 | T (Tampering — decision drift) | A future contributor changes the UX-01 decision without re-opening the checkpoint | mitigate | The PROJECT.md Key Decisions row names the decision explicitly and links to 06-CONTEXT.md for full rationale. Changing the decision requires a new phase (via `/gsd-phase insert`) — not a silent commit. |
| T-06-01-03 | R (Repudiation — no audit trail for the chosen option) | Six months later, "why didn't we ship option (b)?" has no traceable answer | mitigate | The 06-CONTEXT.md `<decisions>` section + the PROJECT.md row + the README callout form a three-way audit trail. The rejected options' costs are explicitly enumerated. |
| T-06-01-04 | D (Denial of Service — spec false-positive) | The flexible toast selector `[role=alertdialog], [role=dialog], .toast` matches Phase 5's toast AND any unrelated dialog | accept | The text filter `/display.*flicker/i` is the disambiguator — only a toast containing both keywords matches. False positive requires a non-display-related dialog to also contain "display" and "flicker", which is operationally unlikely. |
| T-06-01-05 | E (Elevation — surprise recreate) | Operator clicks Update, toast does not fire (Phase 5 substring detection regression), recreate happens immediately, screen blacks out | mitigate | Task 2's cancel-path test asserts no POST fires before confirmation; if substring detection regresses, the test fails and CI blocks the regression from merging. |
| Pitfall 5 (display blackout) | I/D | Operator surprise on flutter/weston recreate | mitigate | Documented in README "Before you click Update on flutter or weston"; Phase 5 toast is the in-product mitigation; Rollback is the safety net. |

</threat_model>

<verification>
- `grep -F 'UX-01' PROJECT.md` matches the Key Decisions row
- `test -f README.md` is true and `grep -F 'Before you click Update on flutter' README.md` matches
- `e2e/tests/weston-warning.spec.ts` exists, contains both test cases
- `e2e/compose.test.yml` has a `weston-stub` service with `hmi-update.watch=true`
- `make e2e` exits 0 with the new spec included
- Manual smoke: open the UI in a browser against the e2e stack, click Update on weston-stub, observe the toast fire with "display" + "flicker" wording, click Cancel, confirm no recreate occurred (compose stack's weston-stub container's start time unchanged)
- No regression in other phase e2e specs
- PROJECT.md is append-only — existing Key Decisions rows preserved
</verification>

<success_criteria>
1. **UX-01 decision recorded:** PROJECT.md Key Decisions table has a new row naming option (a) with the canonical rationale; reviewer in six months can see exactly why options (b) and (c) were rejected.
2. **UX-02 documentation shipped:** README.md exists at repo root with the load-bearing "Before you click Update on flutter or weston" section naming the 5-30s blackout window, the Phase-5 pre-action toast, and the Rollback safety net.
3. **UX-02 contract pinned in CI:** `e2e/tests/weston-warning.spec.ts` is RED-first and goes green against Phase 5's UI-08 implementation; future Phase 5 regressions fail this spec.
4. **UX-03 explicitly NOT shipped:** No `prepared_digest` state schema field, no third per-row button, no new endpoint. Phase 6 closes UX-03 as "not applicable — option (a) chosen."
5. **Manual smoke confirms toast fires** for `weston-stub` in a real browser session; cancel path leaves the container untouched.
6. **Phase 7 seed established:** README.md is the file Phase 7 extends with the full install runbook (DEPLOY-08 / Pitfall 9 — `id -g docker`, compose deployment block). Phase 6 creates it; Phase 7 extends it.
</success_criteria>

<output>
After completion, create `.planning/phases/06-display-blackout-ux-checkpoint/06-01-SUMMARY.md` documenting:
- UX-01 decision: option (a) chosen; verbatim rationale paragraph; references to PROJECT.md row and README.md callout
- UX-02 deliverable shape: README.md content sections; key paraphrase choices (5-30s blackout, weston cascade, Rollback safety net, Force Pull recovery)
- UX-03 disposition: NOT applicable under option (a); deferred to v2 follow-up if operator feedback indicates the toast is insufficient
- Spec coverage: weston-warning.spec.ts test cases (happy path + cancel path); the selector strategy (flexible role/text fallback)
- Phase 5 → Phase 6 contract: substring detection on `flutter` / `weston` in service name (case-insensitive); toast keywords "display" + "flicker"
- Manual smoke note: what the operator observed in the browser against the e2e stack
- Open notes for Phase 7: README.md is now the seed for the full install runbook; Phase 7 will append the `id -g docker` install step, the compose deployment block (DEPLOY-04), and the self-upgrade procedure pointer
- Open notes for v2: option (b) two-step prepare/switch as a deferred candidate if operator feedback requires it
</output>
