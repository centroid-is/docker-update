---
phase: 08-ci-cd-release-hardening
plan: 03
type: execute
wave: 3
depends_on: [08-01, 08-02]
files_modified:
  - RELEASING.md
  - SMOKE.md
  - .planning/PROJECT.md
autonomous: true
requirements: [CI-05]
must_haves:
  truths:
    - "`RELEASING.md` at repo root documents the full release process: pre-release checklist + manual-smoke procedure + tagging step + manual-smoke record (SMOKE.md)"
    - "`SMOKE.md` at repo root is an append-only ledger; each entry has date, :sha-<short> tested, host OS+docker version, result note"
    - "PROJECT.md cross-links RELEASING.md from its Key Decisions / Release Process section so the file is discoverable from the canonical project doc"
    - "RELEASING.md's manual-smoke procedure references Phase 7's install runbook (the brief's success criterion #5 — `:sha-<short>` pulled from GHCR runs cleanly under the Phase 7 runbook)"
    - "RELEASING.md's tagging step uses `git tag -s` (signed annotated tag) and references the `v*.*.*` semver pattern that publish.yml's tag-push trigger matches"
    - "The release process explicitly forbids tagging without (a) green CI on main AND (b) a recorded SMOKE.md entry — these are the C4 'done' criteria"
    - "A dry-run verification (Procedure D below) confirms the documented flow works without actually publishing a release"
  artifacts:
    - path: RELEASING.md
      provides: "operator-readable release runbook"
      contains: "manual-smoke"
    - path: SMOKE.md
      provides: "append-only manual-smoke record ledger"
      contains: "## Smoke records"
    - path: .planning/PROJECT.md
      provides: "cross-link to RELEASING.md from Key Decisions / Release Process"
      contains: "RELEASING.md"
  key_links:
    - from: RELEASING.md
      to: SMOKE.md
      via: "append-only manual-smoke record"
      pattern: "SMOKE.md"
    - from: RELEASING.md
      to: .github/workflows/publish.yml
      via: "`v*.*.*` tag-push trigger semantics"
      pattern: "v*.*.*"
    - from: .planning/PROJECT.md
      to: RELEASING.md
      via: "Key Decisions / Release Process cross-link"
      pattern: "RELEASING.md"
---

<objective>
Ship the release process documentation that closes Phase 8's C4 loop: an operator-readable
`RELEASING.md` at repo root + an append-only `SMOKE.md` ledger + a cross-link from PROJECT.md.
Verify the documented flow works end-to-end via a dry-run (`gh release create --draft`) on a
feature branch.

Purpose: Phase 8's CI + publish workflows (08-01 + 08-02) produce green-CI-or-red automatically.
But the brief's C4 constraint ("manual smoke on an HMI-like stack is required before 'done'")
is a HUMAN gate, not a workflow gate. This plan documents the human gate so a maintainer
hand-tagging a release knows the prerequisites, performs them, and records the evidence
where a future audit can find it.

Without this plan, Phase 8 ships the technical machinery but leaves the C4 process implicit.
The brief's success criterion #4 — "release process documents the manual-smoke gate" — would
not be met.

Output: Two new repo-root files (RELEASING.md, SMOKE.md), one PROJECT.md update, and a
recorded dry-run that proves the documented procedure works.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.planning/phases/08-ci-cd-release-hardening/08-CONTEXT.md
@.planning/phases/08-ci-cd-release-hardening/08-RESEARCH.md

# The CI/publish workflows this doc references (output of 08-01 + 08-02):
@.github/workflows/ci.yml
@.github/workflows/publish.yml

# Phase 7's install runbook (referenced by the manual-smoke procedure; not modified):
# (Lives in README.md after Phase 7 lands — this plan reads/cross-links the section
#  that Phase 7's parallel plan agent is producing.)
@README.md
</context>

<interfaces>
<!-- 08-03 produces two new files at repo root and updates one .planning/ file. -->

`RELEASING.md` is the runbook. Its sections (in order):

1. **Overview** — one paragraph: what this file is for, why it exists (C4 brief constraint),
   who reads it (Centroid field maintainers tagging a release).
2. **Pre-release checklist** — bulleted list of preconditions.
3. **Manual-smoke procedure** — numbered steps an operator runs on an HMI-like stack.
4. **Recording the smoke** — pointer to SMOKE.md + a template entry.
5. **Tagging the release** — the actual `git tag -s vX.Y.Z` + `git push origin vX.Y.Z`
   commands, plus what to expect from GitHub Actions afterward.
6. **What to do if the smoke fails** — rollback / re-cut procedure.
7. **Where to find evidence** — links to the publish workflow run URL, the SMOKE.md entry,
   and the GHCR package page.

`SMOKE.md` is a tiny append-only ledger. Header explains what it is; each entry is a short
markdown subsection with the four required fields (date, sha, host, result note).

PROJECT.md gets ONE new bullet under its "Key Decisions" or "Release Process" section
(whichever already exists, or a new section if neither does) cross-linking RELEASING.md.

The dry-run verification (Task 2) uses `gh release create --draft` to simulate the GitHub
Release UI side-effects without actually publishing a release — proves the flow lands as
documented.

</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ship RELEASING.md + SMOKE.md + PROJECT.md cross-link</name>
  <files>RELEASING.md, SMOKE.md, .planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (the current shape — identify where the cross-link goes; the file's existing structure dictates whether we add a bullet to "Key Decisions" or a new "Release Process" subsection)
    - .planning/phases/08-ci-cd-release-hardening/08-CONTEXT.md (Area 4 — release process and manual-smoke gate; defines the exact shape of RELEASING.md and SMOKE.md)
    - .github/workflows/publish.yml (output of 08-02 — RELEASING.md references the tag-push trigger semantics and what publish workflow run an operator should expect)
    - .github/workflows/ci.yml (output of 08-01 — RELEASING.md references the green-CI prerequisite)
    - README.md (Phase 7's install runbook lives here after Phase 7 lands — the manual-smoke procedure cross-links it; if Phase 7 hasn't shipped yet, leave a TODO with the section anchor expected from Phase 7's plan)
    - .planning/research/PITFALLS.md (Pitfall 5 — display blackout; manual-smoke procedure picks a non-display-blackout target per Pitfall 5 guidance)
  </read_first>
  <behavior>
    - `RELEASING.md` exists at repo root, is operator-readable, follows the six-section structure in Interfaces.
    - `SMOKE.md` exists at repo root with a header + empty (or seed-entry) records section.
    - PROJECT.md has a new cross-link bullet pointing at `RELEASING.md` from its decisions / release section.
    - All three files commit cleanly (no trailing whitespace, no markdownlint errors at the project's existing strictness level).
  </behavior>
  <action>
    Create `RELEASING.md` at repo root with the following content:

    ```markdown
    # Releasing hmi-update

    > **Purpose:** This runbook documents how a Centroid field maintainer cuts a release of
    > `hmi-update`. The brief's C4 constraint requires a manual smoke on an HMI-like stack
    > before a release is "done"; this file is the human-side counterpart to the automated
    > CI/publish workflows in `.github/workflows/`.

    > **Audience:** Maintainers with `centroid-is/hmi-update` write access who tag releases.

    > **What you'll do:** Confirm pre-release prerequisites → manually smoke a candidate
    > `:sha-<short>` image on an HMI-like stack → record the result in `SMOKE.md` → tag the
    > release → confirm the publish workflow ran green.

    ---

    ## 1. Pre-release checklist

    Before tagging a release, every box below must be checked. Skipping any box is a
    deliberate exception that should be recorded in the SMOKE.md entry.

    - [ ] All v1 phase plans complete (verify with `cat .planning/STATE.md`).
    - [ ] Latest commit on `main` shows a green `ci` workflow run on GitHub Actions.
    - [ ] Latest commit on `main` shows a green `publish` workflow run on GitHub Actions —
          the candidate `:sha-<short>` is at `ghcr.io/centroid-is/hmi-update:sha-<short>`.
    - [ ] Local `make test-sigkill` passes (Phase 4 STATE-04 SIGKILL fault injection —
          deliberately not in CI per Phase 4 design).
    - [ ] `README.md`'s install runbook reflects the current install procedure (Pitfall 9
          `id -g docker` step, three bind-mounts, etc.).
    - [ ] No uncommitted edits on your `main` checkout (`git status` clean).
    - [ ] You are on an HMI or an HMI-like stack (a Debian 12 box with Docker + Compose v2.20+
          installed, matching the production runtime — NOT a developer macOS / WSL2 box).

    ## 2. Manual-smoke procedure

    > **Why this exists:** Even with green CI and a passing publish workflow, the only way to
    > confirm a release runs cleanly on real hardware is to run it on real hardware. This
    > procedure exercises the install runbook plus the Update → Rollback round trip — the
    > headline differentiator from WUD.
    >
    > **Display-blackout warning (Pitfall 5):** Do NOT pick `flutter` or `weston` as the
    > smoke target — recreating them blanks the operator's screen. Pick a non-display-drawing
    > service: a database (timescaledb labeled `hmi-update.allow-update=false` is fine for
    > read-only checks, but pick a service WITHOUT `allow-update=false` for the actual
    > Update/Rollback exercise), a sidecar, or a known-safe canary container.

    Steps:

    1. **Identify the candidate `:sha-<short>` tag** — the most recent `sha-` tag at
       `https://github.com/centroid-is/hmi-update/pkgs/container/hmi-update` matching the
       commit at the tip of `main`. Verify locally:
       ```bash
       crane digest ghcr.io/centroid-is/hmi-update:sha-$(git rev-parse --short HEAD)
       ```
       The digest must be non-empty.

    2. **Pull on the HMI-like stack:**
       ```bash
       docker pull ghcr.io/centroid-is/hmi-update:sha-$(git rev-parse --short HEAD)
       ```

    3. **Follow the Phase 7 install runbook** at `README.md#install` (or its current
       section anchor). The runbook covers:
       - `id -g docker` and setting `user: "65532:<docker-gid>"` in compose.
       - The three bind mounts (`/var/run/docker.sock`, `docker-compose.yml:ro`,
         `hmi_update_state.json`).
       - The env vars (`HMI_UPDATE_CRON`, `HMI_UPDATE_COMPOSE_PATH`).
       - `docker compose up -d hmi-update`.

    4. **Verify the UI loads:** open `http://<hmi-like-host>:8080/` in a browser.
       Expected: the watched-containers table renders with at least one row.

    5. **Verify `/api/state`:**
       ```bash
       curl -fsS http://<hmi-like-host>:8080/api/state | jq '.version'
       ```
       Expected: `1`.

    6. **Exercise Update → Rollback round trip** on a non-display-blackout service:
       a. Click `Update` on the chosen row. EXPECTED: container recreates within 30s, the
          UI's `current_digest` flips, `previous_digest` populates.
       b. Click `Rollback` immediately afterward. EXPECTED: container recreates within 15s
          back to the previous digest; `update_available` flips back on (because the
          registry `:latest` is still the newer digest).

    7. **Capture evidence:** a screenshot of the UI showing both digests + the action timestamps
       in `slog` output (`docker logs hmi-update | tail -50`).

    ## 3. Recording the smoke

    Append a new entry to `SMOKE.md` with the following template:

    ````markdown
    ### vX.Y.Z — YYYY-MM-DD

    - **Candidate tag:** `ghcr.io/centroid-is/hmi-update:sha-<short>`
    - **Image digest:** `sha256:<digest>` (from `crane digest`)
    - **Host:** Debian 12 / Docker Engine vXX.X / Compose vX.XX
    - **Operator:** <name> <email>
    - **Result:** PASS / PASS-WITH-NOTES / FAIL
    - **Notes:** <one-paragraph summary — what was tested, anything unexpected>
    ````

    The entry is the C4 "recorded manual smoke note." A release is NOT cut until the entry is
    committed to `main` on a PR titled `release: vX.Y.Z prep`. The PR may include README /
    CHANGELOG updates that are part of the release; the SMOKE.md entry is the one
    required addition.

    ## 4. Tagging the release

    Once SMOKE.md is committed on `main`:

    ```bash
    git checkout main
    git pull origin main
    # Confirm HEAD matches the :sha-<short> you smoked.
    git rev-parse --short HEAD
    git tag -s -a vX.Y.Z -m "release vX.Y.Z

    See SMOKE.md for the manual-smoke record."
    git push origin vX.Y.Z
    ```

    Notes:
    - `git tag -s` produces a SIGNED annotated tag. Configure GPG signing per the GitHub
      docs (Settings → SSH and GPG keys) if you haven't already. An unsigned tag is
      accepted but loses the trust-on-merge signal that a signed tag carries.
    - The tag name pattern MUST match `v[0-9]+.[0-9]+.[0-9]+` (or `vX.Y.Z-<pre>`). The
      `publish.yml` workflow's `push: tags: ['v*.*.*']` trigger fires on the tag push.
    - Within ~6–8 minutes, the publish workflow run completes; the image is tagged at
      `ghcr.io/centroid-is/hmi-update:X.Y.Z` (no `v` prefix per `metadata-action`'s
      `pattern={{version}}`) AND at `ghcr.io/centroid-is/hmi-update:sha-<short>` (identical
      to the candidate).
    - Verify with:
      ```bash
      crane digest ghcr.io/centroid-is/hmi-update:X.Y.Z
      ```
      The digest must equal the candidate's digest (proves the tag-push publish built the
      same source tree as the main publish, modulo cache).

    ## 5. What to do if the smoke fails

    If step 6 of §2 fails (Update/Rollback misbehaves) or any acceptance check in step 3-5
    fails:

    1. **Do NOT tag.** Stop the release.
    2. Open a bug issue with the failure mode + the `docker logs hmi-update` excerpt.
    3. Either:
       a. Roll back the offending commit on `main` and let the next green CI produce a new
          `:sha-<short>` candidate, OR
       b. Land a fix on `main`, wait for green CI, and restart this runbook from §1.
    4. Optionally: record the failed attempt in SMOKE.md with a `FAIL` result and notes — keeps
       the trail intact for a future audit ("we tried at <sha>, it failed because X, the fix
       went in at <next-sha>"). This is recommended for non-trivial failures.

    ## 6. Where to find evidence after release

    For a tagged release `vX.Y.Z`:

    - **CI run:** the green `ci.yml` workflow run on the merge commit before the tag.
      `https://github.com/centroid-is/hmi-update/actions/workflows/ci.yml`
    - **Publish run:** the green `publish.yml` workflow run triggered by the tag push.
      `https://github.com/centroid-is/hmi-update/actions/workflows/publish.yml`
    - **Manual-smoke record:** the `SMOKE.md` entry for vX.Y.Z.
    - **Image on GHCR:** `https://github.com/centroid-is/hmi-update/pkgs/container/hmi-update`
      — confirm `:X.Y.Z`, `:latest`, and `:sha-<short>` all point to the same digest.

    ---

    *Last updated: Phase 8 plan 08-03 (2026-05-15).*
    *See `.planning/phases/08-ci-cd-release-hardening/08-CONTEXT.md` for the locked design
    decisions behind this runbook.*
    ```

    Create `SMOKE.md` at repo root with the following content:

    ```markdown
    # Manual-smoke records

    > **What this file is:** an append-only ledger of manual smoke runs that gate releases of
    > `hmi-update`. Every entry corresponds to a release tag or a candidate that was tested
    > but not tagged (e.g. a FAIL run that was rolled back).
    >
    > **Why it exists:** the brief's C4 process constraint requires a recorded manual-smoke
    > note before a release is "done." This file is that record. It is committed to `main`
    > as part of the release PR; the git history of this file is the audit trail.
    >
    > **How to add an entry:** see `RELEASING.md` §3.

    ## Smoke records

    <!-- Add new entries at the TOP of this section (most-recent-first). -->

    *(no entries yet — first entry lands with vX.Y.Z)*
    ```

    Update `.planning/PROJECT.md`:

    Locate the existing "Key Decisions" section (or the section that lists project-wide
    process commitments — read PROJECT.md to identify the exact heading). Add a bullet or
    a small new subsection cross-linking RELEASING.md:

    - If PROJECT.md has a "Key Decisions" table: add a row.
    - If PROJECT.md has a "Release Process" section: add the link there.
    - If PROJECT.md has neither: add a new "Release Process" section near the end, with
      the link.

    Suggested text (adapt to the file's existing style):

    ```markdown
    ## Release Process

    Releases are tagged from `main` after a green CI run AND a recorded manual smoke on an
    HMI-like stack. The full runbook lives at [RELEASING.md](../../RELEASING.md); the
    append-only smoke ledger is [SMOKE.md](../../SMOKE.md). The brief's C4 constraint
    ("manual smoke on an HMI-like stack is required before 'done'") is enforced by this
    process, not by any CI gate.
    ```

    Constraints to honor:
    - RELEASING.md uses the exact section structure in Interfaces (six numbered sections).
    - All cross-links use repo-root-relative paths (no http URLs for files in the same repo,
      EXCEPT for the GHCR package page and the GitHub Actions workflow run pages, which are
      legitimately off-repo).
    - The Pitfall 5 display-blackout warning appears in §2 — it's load-bearing for the
      manual-smoke procedure.
    - The smoke template includes ALL four required fields: date, candidate tag, host, result.
    - The tag command uses `-s -a` (signed annotated). If GPG signing isn't configured on the
      maintainer's machine, the doc explicitly notes the fallback (unsigned `-a` is accepted
      but loses the signal).
    - The tagging step references the `v[0-9]+.[0-9]+.[0-9]+` regex EXPLICITLY — operators
      reading the doc should not have to look up the trigger config in publish.yml.
    - PROJECT.md is touched MINIMALLY — one bullet/section. Do not refactor the file's
      existing structure.
  </action>
  <verify>
    <automated>
      # 1. Files exist:
      test -f RELEASING.md && \
      test -f SMOKE.md && \
      # 2. RELEASING.md has the six sections:
      grep -E '^## 1\\.' RELEASING.md && \
      grep -E '^## 2\\.' RELEASING.md && \
      grep -E '^## 3\\.' RELEASING.md && \
      grep -E '^## 4\\.' RELEASING.md && \
      grep -E '^## 5\\.' RELEASING.md && \
      grep -E '^## 6\\.' RELEASING.md && \
      # 3. Pitfall 5 reference:
      grep -E 'Pitfall 5|display-blackout|display blackout' RELEASING.md && \
      # 4. SMOKE.md ledger structure:
      grep -E '^## Smoke records' SMOKE.md && \
      # 5. PROJECT.md cross-link:
      grep -E 'RELEASING\\.md' .planning/PROJECT.md && \
      # 6. Tag-pattern reference:
      grep -E 'v\\[0-9\\]\\+\\.\\[0-9\\]\\+\\.\\[0-9\\]\\+|v\\*\\.\\*\\.\\*' RELEASING.md && \
      # 7. SMOKE.md template includes the four fields:
      grep -E 'Candidate tag:' RELEASING.md && \
      grep -E 'Host:' RELEASING.md && \
      grep -E 'Result:' RELEASING.md && \
      grep -E 'Notes:' RELEASING.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `RELEASING.md` exists at repo root with the six sections from Interfaces (Pre-release checklist / Manual-smoke procedure / Recording the smoke / Tagging the release / What to do if smoke fails / Where to find evidence).
    - `SMOKE.md` exists at repo root with a header explaining its purpose and a "Smoke records" section that is empty/placeholder.
    - `.planning/PROJECT.md` has a new cross-link to `RELEASING.md` (location depends on the file's existing structure — minimal-touch).
    - The manual-smoke procedure in RELEASING.md §2 references Phase 7's install runbook (via `README.md#install` or whatever anchor Phase 7's parallel plan agent produces — if Phase 7 hasn't shipped, leave a `<!-- TODO(phase-7): confirm anchor -->` comment with the expected target).
    - The Pitfall 5 display-blackout warning appears in §2 (verified via `grep -E 'Pitfall 5|display-blackout|display blackout' RELEASING.md`).
    - The tagging step (§4) explicitly references the `v[0-9]+.[0-9]+.[0-9]+` (or `v*.*.*`) tag pattern that publish.yml triggers on.
    - The tag command uses `git tag -s -a vX.Y.Z -m "..."` (signed annotated tag).
    - The smoke template includes Candidate tag / Image digest / Host / Operator / Result / Notes fields (matching CONTEXT.md Area 4).
    - `markdownlint` (if the project runs it — not required) does not flag malformed structure.
    - `git diff PROJECT.md` shows a small additive change only (no structural refactor).
  </acceptance_criteria>
  <done>
    Three files (two new, one modified) commit cleanly. RELEASING.md is operator-readable,
    references the publish workflow's trigger semantics, includes the Pitfall 5 warning, and
    documents the SMOKE.md ledger structure. PROJECT.md cross-links to RELEASING.md so the
    file is discoverable from the canonical project doc.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dry-run verification of the documented flow via `gh release create --draft`</name>
  <files>(none committed — recorded outcomes in 08-03-SUMMARY.md)</files>
  <read_first>
    - RELEASING.md (output of Task 1)
    - SMOKE.md (output of Task 1)
    - .github/workflows/publish.yml (output of 08-02 — for understanding what the dry-run is simulating)
  </read_first>
  <behavior>
    - A dry-run on a feature branch produces a draft GitHub Release that the maintainer can preview, edit, and ultimately discard — proving the documented `git tag -s` step works on the maintainer's machine without committing to an actual release.
    - The dry-run does NOT push a tag to origin and does NOT trigger publish.yml. (`gh release create --draft` against a non-existent tag creates the tag locally only if `--target` is omitted; otherwise it operates against a target commit without creating the tag.)
    - A SMOKE.md template entry is drafted as part of the dry-run (committed to the feature branch, NOT to main) — proves the entry shape is correct.
  </behavior>
  <action>
    The dry-run is a procedure the executor performs once. Outcomes recorded in 08-03-SUMMARY.md.

    Procedure D — release dry-run:

    1. From `main`: `git checkout -b release-dry-run/phase-8-verify`.

    2. Draft a SMOKE.md entry on this branch (using the v0.0.0-test tag from 08-02 Procedure B
       as the candidate; do NOT use a real `vX.Y.Z` number):
       ```markdown
       ### v0.0.0-test — 2026-05-15

       - **Candidate tag:** `ghcr.io/centroid-is/hmi-update:sha-<short-from-08-02>`
       - **Image digest:** `sha256:<digest-from-08-02-procedure-b>`
       - **Host:** dry-run on developer machine — NOT a real HMI smoke; do not treat as evidence of v0.0.0-test fitness.
       - **Operator:** Plan 08-03 verification executor
       - **Result:** PASS-WITH-NOTES (dry-run only; structural verification of the SMOKE.md template, not an actual smoke)
       - **Notes:** This entry was produced by Plan 08-03 Task 2 as a dry-run of the documented release flow. The actual v0.0.0-test tag from 08-02 Procedure B is a verification artifact, not a real release. Discarded on branch close.
       ```
       Commit: `git add SMOKE.md && git commit -m "test(release): dry-run SMOKE.md entry (do not merge)"`.

    3. Run the `gh release create --draft` command exactly as it WOULD be run for a real
       release, but against the `v0.0.0-test` tag from 08-02 Procedure B (which exists in
       origin's history if 08-02 Procedure B was executed; if not, skip to step 5 with a
       static-verification note instead):
       ```bash
       gh release create v0.0.0-test \
         --draft \
         --title "v0.0.0-test (dry-run — do not merge)" \
         --notes "Plan 08-03 Task 2 dry-run. See SMOKE.md."
       ```
       EXPECTED: a draft release is created on GitHub; URL is returned.
       Record the draft release URL in 08-03-SUMMARY.md.

    4. View the draft release in the GitHub UI to confirm:
       - The release notes display correctly.
       - The "Assets" section shows the source code archives (auto-attached).
       - The draft is NOT visible to non-maintainers (the `--draft` flag is honored).

    5. Delete the draft release:
       ```bash
       gh release delete v0.0.0-test --yes
       ```
       (This deletes the draft only; the Git tag itself was already deleted in 08-02 Procedure B
       cleanup.)

    6. Discard the dry-run branch:
       ```bash
       git checkout main
       git branch -D release-dry-run/phase-8-verify
       # No push — branch was never pushed to origin.
       ```

    7. Record in 08-03-SUMMARY.md:
       - The draft-release URL (from step 3).
       - A confirmation that the documented `gh release create --draft` command works as
         specified.
       - A note on whether the SMOKE.md template fields all displayed correctly in the
         drafted entry.
       - Any deviations from the documented procedure (e.g. if the executor's `gh` CLI version
         differs from a recently-documented `gh release create` flag, update RELEASING.md to
         match).

    NOTE: If 08-02 Procedure B was skipped (no `v0.0.0-test` tag in origin), the dry-run
    can be performed against `HEAD` instead, using `gh release create --draft v0.0.0-dry-run
    --target $(git rev-parse HEAD)`. The static verification (RELEASING.md says what the
    command would do) is enough evidence in that case.
  </action>
  <verify>
    <automated>
      # No automated verify — outcomes are in 08-03-SUMMARY.md.
      # Static check: RELEASING.md is structurally complete.
      grep -E '^## 4\\. Tagging' RELEASING.md && \
      grep -E 'gh release create' .planning/phases/08-ci-cd-release-hardening/08-03-PLAN.md ; \
      # The gh release create command is in this PLAN file, not RELEASING.md — RELEASING.md
      # documents the GIT TAG step, not the GitHub Release notes step (those are out-of-scope
      # per 08-CONTEXT.md `<deferred>`). The dry-run verifies the documented git tag flow
      # would work end-to-end if the maintainer chose to use `gh release create` for the
      # release notes phase.
      echo "static verification complete"
    </automated>
  </verify>
  <acceptance_criteria>
    - 08-03-SUMMARY.md records the draft release URL (or a static-verification note if 08-02 Procedure B was skipped).
    - 08-03-SUMMARY.md confirms the documented `git tag -s -a vX.Y.Z` command syntax is correct for the executor's local Git + GPG configuration (or notes a fallback to unsigned tags if GPG isn't configured).
    - 08-03-SUMMARY.md confirms the SMOKE.md template fields render correctly in markdown.
    - No `release-dry-run/phase-8-verify` branch exists on origin or locally after Plan 08-03 completes.
    - No `v0.0.0-test` draft release exists on GitHub after Plan 08-03 completes (cleanup in step 5 + the underlying tag cleanup from 08-02 Procedure B).
    - Any deviations between the documented `gh release create` flow and the executor's actual experience are recorded as RELEASING.md updates in a follow-up commit (or as a deferred issue if not blocking).
  </acceptance_criteria>
  <done>
    The release process documented in RELEASING.md has been exercised end-to-end (or
    statically verified). The SMOKE.md template renders correctly. The flow is ready for a
    real first-release.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RELEASING.md → maintainer's understanding | A misleading doc could direct an operator to skip the manual-smoke or use an unsafe tag pattern. Mitigated by reviewing RELEASING.md changes via PR. |
| SMOKE.md → audit trail | An append-only ledger committed to `main` is the C4 evidence. A maintainer skipping the SMOKE.md entry bypasses C4 silently. Mitigated by branch-protection rules that require a SMOKE.md change in a "release: vX.Y.Z prep" PR (org-level config, out of v1 scope). |
| PROJECT.md cross-link → discoverability | A broken cross-link makes RELEASING.md hard to find. Mitigated by relative-path link + Task 1 verification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Tampering | A future PR rewrites RELEASING.md to remove the manual-smoke gate | mitigate | The file is in `files_modified` of this plan; changes are visible in PR diffs. V2 hardening: a CODEOWNERS rule on RELEASING.md + SMOKE.md. |
| T-08-03-02 | Repudiation | A maintainer disputes that a release was manually smoked | mitigate | SMOKE.md is committed to `main` and git history is the audit trail. Even a force-push (if the org allowed it) leaves the reflog. |
| T-08-03-03 | Information Disclosure | SMOKE.md entries include sensitive HMI information | accept | Entries are operator-name + host OS version + smoke result. No customer data, no IP addresses, no secrets. Worst case: a competitor learns we ran a release on Debian 12. Public benefit (audit trail) outweighs. |
| T-08-03-04 | Spoofing | A maintainer signs a tag with a stolen GPG key | accept | GPG key compromise is out of v1's threat model. Org-level enforcement of signed tags is the V2 hardening (currently `git tag -s` is documented as preferred, but unsigned tags are accepted in v1 — a deliberate trade for usability while GPG onboarding catches up across the team). |
| T-08-03-05 | Denial of Service | RELEASING.md is so cumbersome that maintainers skip steps | mitigate | The doc's six-section structure is intentionally compact (~one screen per section). Step 6 of the manual smoke (screenshot capture) is optional in v1 — recorded in SMOKE.md notes if the operator chose to capture; not a blocker. |
| T-08-03-06 | Elevation of Privilege | A maintainer tags a release from a non-main branch | mitigate | The publish workflow accepts any tag matching `v*.*.*`, regardless of branch ancestry. RELEASING.md §4 explicitly says to tag from `main`. Branch-protection enforcement is org-level, V2 hardening. |
| T-08-03-07 | Tampering | An operator appends a fake PASS to SMOKE.md without running the smoke | accept | Trust-on-commit; SMOKE.md is the operator's word. v1 accepts the trust boundary because the team is small. V2 hardening: tie SMOKE.md entries to signed tags + require the entry's `sha-<short>` to match a real publish workflow run URL. |

(No Information Disclosure threats from the dry-run — `gh release create --draft` keeps the
release invisible to non-maintainers, and the dry-run branch never reaches origin.)
</threat_model>

<verification>
- RELEASING.md, SMOKE.md exist at repo root and parse as valid markdown.
- RELEASING.md has six numbered sections (verified by `grep -E '^## [1-6]\\.'`).
- RELEASING.md references Pitfall 5 / display-blackout (verified by grep).
- RELEASING.md's tag command uses `git tag -s -a vX.Y.Z` and references the `v*.*.*` trigger.
- SMOKE.md has the "Smoke records" section and a placeholder "no entries yet" line.
- .planning/PROJECT.md contains a relative-path link to RELEASING.md.
- The dry-run procedure in Task 2 records a draft release URL (or a static-verification note) in 08-03-SUMMARY.md.
- No `v0.0.0-test` draft release lingers on GitHub after Plan 08-03 completes.
- The SMOKE.md template fields (Candidate tag / Image digest / Host / Operator / Result / Notes) all appear in the runbook.
</verification>

<success_criteria>
- RELEASING.md documents the full release process: pre-release checklist + manual-smoke procedure + tagging step + recording the smoke + failure response + evidence trail.
- SMOKE.md is an append-only ledger with the four-field entry template.
- PROJECT.md cross-links RELEASING.md so the runbook is discoverable from the canonical project doc.
- The Pitfall 5 display-blackout warning appears in the manual-smoke procedure (load-bearing for operator safety).
- The tag command syntax is signed annotated by default with a documented unsigned fallback.
- The `v*.*.*` trigger pattern is explicitly named so operators know what tag string fires the publish workflow.
- The flow has been dry-run verified (or statically verified) end-to-end.
- C4 brief constraint is now process-documented: a release is only tagged after green CI + a recorded SMOKE.md entry.
- Phase 8 success criterion #4 — "release process documents the manual-smoke gate" — is satisfied by RELEASING.md.
- Phase 8 success criterion #5 — "manual smoke on an HMI-like stack confirms a new :sha-<short> image pulled from GHCR runs cleanly under the Phase 7 install runbook" — is now achievable in practice; the procedure is in §2.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ci-cd-release-hardening/08-03-SUMMARY.md`
following the gsd summary template. Include:
- The draft release URL from Task 2 (or static-verification note).
- Confirmation that the documented `git tag -s -a` syntax works on the executor's machine.
- Any RELEASING.md follow-up updates needed based on dry-run findings.
- A pointer to the SMOKE.md placeholder entry (or its absence — both are acceptable for v1).
</output>
