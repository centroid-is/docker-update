---
phase: 09-architectural-hardening-post-v0-1-bug-cluster
plan: 02
type: tdd
wave: 2
depends_on: [09-01-PLAN.md]
files_modified:
  - internal/actions/orchestrator_test.go
  - internal/recreate/translate_test.go
  - internal/api/handlers_self_test.go
  - e2e/relative-bind-mount.spec.ts
  - e2e/compose.test.yml
autonomous: true
requirements: [SC-6, SC-2]
must_haves:
  truths:
    - "Four RED-first regression tests exist, one per bug class from SC-6: (i) compose_file_moved 412, (ii) COMPOSE_PROJECT_NAME no-env, (iii) ./relative-path bind-mount, (iv) CheckSelfProtection 409 + new bypass"
    - "Each test was committed in a dedicated RED commit BEFORE any production code in Plan 09-03 / 09-04 is written"
    - "The relative-path e2e fixture mirrors flutter's wayland-socket layout (two services with `./test-relative-mount` host-side volumes)"
    - "Tests are RED on the pre-Phase-9 codebase and will go GREEN as Plans 09-03 and 09-04 land"
    - "Per C4, the RED commit is `test(09-02): ...` and Plan 09-03's matching GREEN commit is `feat(09-03): ...` (no squash)"
  artifacts:
    - path: internal/recreate/translate_test.go
      provides: "RED-first table tests for the 13-row InspectResponse→Create translation table from RESEARCH.md Pattern 2"
      contains: "func TestTranslate_HostConfig_Binds_AbsoluteAfterDaemonResolution"
    - path: internal/actions/orchestrator_test.go
      provides: "RED-first regression: post-socket-only Update still emits ErrComposeFileMoved → 412 on drift (SC-6 i)"
      contains: "func TestUpdate_ComposeFileMoved_StillReturns412_PostSocketOnly"
    - path: internal/api/handlers_self_test.go
      provides: "RED-first tests for SC-4 + SC-6 (iv): 202 helper-spawned, bypass CheckSelfProtection, 503 unwired, 409 actions-in-flight"
      contains: "func TestHandleSelfUpdate_202_HelperSpawned"
    - path: e2e/relative-bind-mount.spec.ts
      provides: "RED-first e2e: two services with `./test-relative-mount` volumes; assert recreated container's HostConfig.Binds[0] is absolute and matches host path (SC-2 b, SC-6 iii)"
      contains: "test('relative bind-mount resolves to operator host path"
    - path: e2e/compose.test.yml
      provides: "Fixture services with `./test-relative-mount` volumes mirroring flutter's wayland-socket pattern"
      contains: "./test-relative-mount"
  key_links:
    - from: internal/recreate/translate_test.go
      to: internal/recreate/translate.go (created in Plan 09-03)
      via: "package import — translate_test.go declares `package recreate` and calls `Translate(inspect)` expecting the signature from RESEARCH.md Pattern 2"
      pattern: "package recreate"
    - from: e2e/relative-bind-mount.spec.ts
      to: e2e/compose.test.yml fixture services
      via: "Playwright globalSetup brings up the test stack; spec POSTs Update against fixture service names"
      pattern: "test-relative-mount"
    - from: internal/api/handlers_self_test.go
      to: internal/api/handlers_self.go (created in Plan 09-04)
      via: "Test imports `package api` and asserts on `Server.handleSelfUpdate` route registration via `POST /api/self-update`"
      pattern: "POST /api/self-update"
---

<objective>
Land four RED-first regression tests covering the SC-6 bug classes (compose_file_moved 412, COMPOSE_PROJECT_NAME, ./relative-path bind-mount, CheckSelfProtection 409 / bypass) plus the SC-2 relative-bind-mount e2e fixture, BEFORE any production code in Plans 09-03 / 09-04. Per CLAUDE.md C4 and the HANDOFF.md TDD callout, every Phase 9 production change must have a failing test committed first.

Purpose: The BUG-7 → BUG-7b → BUG-7c → BUG-7d chain from the 2026-05-15/16 session is the worked example of what happens when TDD is skipped. This plan front-loads the RED commits so Plans 09-03 and 09-04 only ship code that turns existing failing tests green.

Output: One commit per test file. Each commit is shaped `test(09-02): add failing test for <bug class>`. The tests reference packages / types / endpoints that DO NOT YET EXIST (e.g. `package recreate`, `POST /api/self-update`) — they will fail at compile or assertion time on the pre-Phase-9 codebase. Plans 09-03 and 09-04 turn them green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/HANDOFF.md
@.planning/phases/09-architectural-hardening-post-v0-1-bug-cluster/09-RESEARCH.md
@.planning/phases/09-architectural-hardening-post-v0-1-bug-cluster/09-VALIDATION.md
@CLAUDE.md
@internal/actions/orchestrator.go
@internal/actions/orchestrator_test.go
@internal/actions/middleware.go
@internal/actions/middleware_test.go
@internal/api/server.go
@internal/api/handlers_actions.go
@internal/api/handlers_actions_test.go
@internal/compose/errors.go
@e2e/compose.test.yml
@e2e/playwright.config.ts

<interfaces>
<!-- Extracted from existing source so tests are written against the real contracts -->
<!-- These are the EXISTING surface the new tests will exercise; the NEW surface (recreate.Translate, handleSelfUpdate) is specified in the action -->

From internal/compose/errors.go:
- `var ErrComposeFileMoved = errors.New(...)` — sentinel for compose-file drift (still emitted post-Phase 9 per RESEARCH.md Open Question 4)

From internal/actions/orchestrator.go (existing — read at task time for exact signatures):
- `type Orchestrator struct { ... }`
- `func NewOrchestrator(deps ...) *Orchestrator`
- `func (o *Orchestrator) Update(ctx context.Context, svc string) error`
- `func (o *Orchestrator) Rollback(ctx context.Context, svc string) error`

From internal/actions/middleware.go (existing):
- `func (o *Orchestrator) CheckSelfProtection(svc string) error` — returns sentinel that maps to 409 for per-service self-target

From internal/api/server.go:
- `type Server struct { ... }`
- `func NewServer(...) *Server` — current signature
- `func (s *Server) Routes() http.Handler` (or similar mux registration)

From RESEARCH.md (NEW interfaces tests must expect to exist post-09-03/09-04):
- `package recreate`: `func Translate(inspect container.InspectResponse) (*container.Config, *container.HostConfig, *network.NetworkingConfig, map[string]*network.EndpointSettings)`
- `package api`: route `POST /api/self-update` → `handleSelfUpdate` → returns 202 with `{"status":"helper_spawned","helper_id":"..."}` on success; 503 when `s.selfUpdater == nil`; 409 when other actions in flight (per Open Question 5); bypasses CheckSelfProtection
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — translate_test.go (13 cases for InspectResponse→Create translation)</name>
  <files>internal/recreate/translate_test.go</files>
  <read_first>
    - `.planning/phases/09-architectural-hardening-post-v0-1-bug-cluster/09-RESEARCH.md` § Architecture Patterns / Pattern 2 (the 13-row translation table — every field is a test case)
    - `.planning/phases/09-architectural-hardening-post-v0-1-bug-cluster/09-RESEARCH.md` § Common Pitfalls (Pitfalls 1, 2, 3 — preserved compose labels, ID-tracking across recreate, multi-network)
    - `internal/docker/_sdk_shape.txt` (verbatim go-doc of moby/moby/client v0.4.1 — defines the exact field shapes the table-tests fixture against)
    - `internal/actions/orchestrator_test.go` (existing test patterns — table-test style, fake docker.Client, slog handler captures)
    - `internal/docker/client.go` (existing `Client` interface — confirm shape of type aliases for `container.InspectResponse`, `container.Config`, `container.HostConfig`)
  </read_first>
  <behavior>
    Every row in RESEARCH.md Pattern 2's translation table becomes a sub-test in a table-driven `TestTranslate`:

    - TestTranslate_HostConfig_Binds_AbsoluteAfterDaemonResolution — fixture inspect.HostConfig.Binds = []string{"/home/centroid/wayland-socket:/run/wayland"}; assert returned HostConfig.Binds == same; verifies SC-2(a) AND SC-6(iii) at unit-test level
    - TestTranslate_HostConfig_Mounts_PassThrough — same shape with `mount.Mount{Type, Source, Target}`
    - TestTranslate_HostConfig_NetworkMode_PassThrough — host / bridge / container:abc / custom-name
    - TestTranslate_HostConfig_RestartPolicy_EmptyNameNormalizedToNo — assert empty string normalized to "no" per RESEARCH.md gotcha #1
    - TestTranslate_Config_Healthcheck_PreservesNilVsEmptyDistinction — three sub-cases: nil pointer, struct with Test=nil, struct with Test=["CMD","..."] per gotcha #2
    - TestTranslate_NetworkSettings_FirstNetworkInNetworkingConfig — two-network fixture; assert returned NetworkingConfig.EndpointsConfig has exactly one key (the first network) per gotcha #3
    - TestTranslate_NetworkSettings_ExtraNetworksReturnedSeparately — same two-network fixture; assert second-and-later networks come back as the fourth return value (map[string]*network.EndpointSettings)
    - TestTranslate_FiltersShortIDAlias — fixture Networks[name].Aliases = []string{"my-svc", "abcd12345678"}; given inspect.Name = "/x" and Container ID short-form matches; assert short-ID alias stripped per gotcha #4
    - TestTranslate_NetworkSettings_PinnedIPPreserved — IPAMConfig != nil with IPv4Address set; assert preserved
    - TestTranslate_NetworkSettings_AutoAssignedIPNotRePinned — IPAMConfig == nil but Networks[name].IPAddress populated; assert NOT re-pinned per gotcha #5
    - TestTranslate_Config_Image_NotImageID — fixture inspect.Config.Image = "flutter:latest" and inspect.Image = "sha256:abc..."; assert Translate's Config.Image == "flutter:latest" per gotcha #6 (the orchestrator overrides this post-pull; Translate must round-trip the operator's reference, not the daemon's resolved ID)
    - TestTranslate_HostConfig_Init_PointerTriState — three cases: nil, &false, &true
    - TestTranslate_Config_Labels_PreservesComposeAndHmiUpdateNamespaces — fixture Labels contains `com.docker.compose.service=svc`, `com.docker.compose.project=p`, `com.docker.compose.project.working_dir=/home/operator`, `hmi-update.watch=true`; assert all four pass through verbatim (Pitfall 1)
    - TestRecreate_NoComposeProjectNameEnvDependency — SC-6(ii) gate; fixture envrionment with no COMPOSE_PROJECT_NAME set; call Translate, assert no panic and no Config.Env entries reference COMPOSE_PROJECT_NAME (Translate is a pure function — it must not read process env)
  </behavior>
  <action>
    Create `internal/recreate/translate_test.go` with `package recreate` declaration. The package does NOT EXIST YET — that is intentional; this commit will fail `go build ./internal/recreate/...` and `go test` will report "no Go files in internal/recreate" (or similar parse error). That is the RED state per C4.

    Test signature expectations (from RESEARCH.md Pattern 2):
      ```go
      // Translate maps container.InspectResponse → ContainerCreate inputs.
      // Returns (config, hostConfig, networkingConfig, extraNetworks).
      func Translate(inspect container.InspectResponse) (
          *container.Config,
          *container.HostConfig,
          *network.NetworkingConfig,
          map[string]*network.EndpointSettings,
      )
      ```

    Use the IMPORT path `github.com/moby/moby/api/types/container` and `.../network` per CLAUDE.md tech stack — NOT `docker/docker/...`. The existing `internal/docker/_sdk_shape.txt` confirms the exact identifiers and shapes.

    Each sub-test:
      - Names use the form `TestTranslate_<Field>_<Behavior>` (table-test pattern from `internal/actions/orchestrator_test.go`).
      - Constructs a fully-zeroed `container.InspectResponse` then sets only the fields under test (avoid panics on uninitialized nested pointers).
      - Calls `Translate(fixture)` and asserts on the relevant return value.
      - Uses `t.Run` for sub-test naming.

    Commit shape (per C4 RED commit): `test(09-02): add RED translation-table tests (13 cases for SC-6 ii + SC-2 a)`.

    Verification of RED state: `go test ./internal/recreate/... 2>&1 | grep -E 'no Go files|cannot find package|undefined: Translate'` returns at least one match.
  </action>
  <verify>
    <automated>
      cd /Users/jonb/Projects/tmp && (go test ./internal/recreate/... 2>&1 | grep -E 'no Go files|cannot find package|undefined' >/dev/null && echo "RED confirmed") && grep -c '^func Test' internal/recreate/translate_test.go | awk '$1 >= 13 { exit 0 } { exit 1 }'
    </automated>
  </verify>
  <acceptance_criteria>
    - File `internal/recreate/translate_test.go` exists.
    - First non-comment line is `package recreate`.
    - `grep -c '^func Test' internal/recreate/translate_test.go` returns at least `13` (one per translation-table row + the no-env test).
    - `grep -c '^func TestTranslate_HostConfig_Binds_AbsoluteAfterDaemonResolution' internal/recreate/translate_test.go` returns `1`.
    - `grep -c '^func TestRecreate_NoComposeProjectNameEnvDependency' internal/recreate/translate_test.go` returns `1`.
    - `grep -c '^func TestTranslate_Config_Labels_PreservesComposeAndHmiUpdateNamespaces' internal/recreate/translate_test.go` returns `1`.
    - File imports `"github.com/moby/moby/api/types/container"` (verified by `grep -c '"github.com/moby/moby/api/types/container"' internal/recreate/translate_test.go` >= 1).
    - File imports `"github.com/moby/moby/api/types/network"` (>= 1).
    - File does NOT import `"github.com/docker/docker/..."` (verified by `! grep 'github.com/docker/docker' internal/recreate/translate_test.go`).
    - `go test ./internal/recreate/...` exits non-zero with stderr containing one of: `no Go files`, `cannot find package`, `undefined: Translate`. This confirms RED state per C4.
    - Commit message starts with `test(09-02):` (verified by `git log -1 --format=%s -- internal/recreate/translate_test.go | grep -c '^test(09-02):'` returning `1`).
  </acceptance_criteria>
  <done>
    Test file committed with 13+ `TestTranslate_*` and `TestRecreate_NoComposeProjectNameEnvDependency` functions. `go test ./internal/recreate/...` fails because the `recreate` package has no implementation files yet. RED commit landed before Plan 09-03 executes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED — orchestrator + handlers_self + e2e relative-bind-mount tests + fixture</name>
  <files>internal/actions/orchestrator_test.go, internal/api/handlers_self_test.go, e2e/relative-bind-mount.spec.ts, e2e/compose.test.yml</files>
  <read_first>
    - `internal/actions/orchestrator_test.go` (existing tests — copy the recordingSender pattern, fake docker.Client setup, and slog handler capture)
    - `internal/actions/middleware_test.go` (existing CheckSelfProtection tests — copy fixture shape)
    - `internal/api/handlers_actions_test.go` (existing handler tests — copy the httptest.NewServer + httptest.ResponseRecorder pattern)
    - `internal/api/server.go` (current `Server` struct + `NewServer` signature — the test must construct a Server with the new `selfUpdater` field as `nil` to exercise 503 case)
    - `e2e/compose.test.yml` (current fixture — append new services without breaking existing ones)
    - `e2e/playwright.config.ts` (test discovery globs — confirm `relative-bind-mount.spec.ts` will be picked up)
    - `.planning/phases/09-architectural-hardening-post-v0-1-bug-cluster/09-RESEARCH.md` § Architecture Patterns / Pattern 5 (CheckSelfProtection bypass design)
    - `.planning/phases/09-architectural-hardening-post-v0-1-bug-cluster/09-RESEARCH.md` § Common Pitfalls / Pitfall 7 (the e2e fixture MUST mirror flutter's `./wayland-socket` pattern, not absolute paths)
    - `.planning/phases/09-architectural-hardening-post-v0-1-bug-cluster/09-RESEARCH.md` § Open Questions (5) — actions_in_flight 409 behavior for self-update
    - `.planning/phases/09-architectural-hardening-post-v0-1-bug-cluster/09-RESEARCH.md` § Code Examples / Example 2 (handler shape with `actionBodySelfUpdaterUnwired` constant)
  </read_first>
  <behavior>
    Four new test artifacts, each landed in its own commit (RED-first per C4):

    **(A) `internal/actions/orchestrator_test.go` — append `TestUpdate_ComposeFileMoved_StillReturns412_PostSocketOnly` (SC-6 i):**
      - Fixture: fake docker.Client + fake compose.Reader that returns `compose.ErrComposeFileMoved` from its `CheckUnchanged`/equivalent boot-time check
      - Call: `o.Update(ctx, "any-svc")`
      - Assert: returned error wraps `compose.ErrComposeFileMoved` (verify via `errors.Is(err, compose.ErrComposeFileMoved)`)
      - Asserts that the post-Phase-9 socket-only path STILL surfaces 412 ErrComposeFileMoved (compose.Reader stays per RESEARCH.md Open Question 4)
      - This test will be GREEN on the current codebase (compose.Reader already emits this) — that is acceptable; the value of the test is REGRESSION lock-in for the SC-6(i) behavior after Plan 09-03 deletes compose.Runner. Document this in a comment block at the top of the test: `// SC-6 (i) regression guard: compose.Reader stays after socket-only refactor (RESEARCH.md Open Question 4).`

    **(B) `internal/api/handlers_self_test.go` (NEW FILE) — four tests covering SC-4 + SC-6 (iv):**
      - `TestHandleSelfUpdate_202_HelperSpawned` — fixture Server with `selfUpdater: fakeSpawner{returnID: "helper-abc"}`; POST /api/self-update; assert 202 and body contains `"status":"helper_spawned"` and `"helper_id":"helper-abc"`
      - `TestHandleSelfUpdate_503_WhenUnwired` — fixture Server with `selfUpdater: nil`; POST /api/self-update; assert 503 and body contains `self_updater_not_wired`
      - `TestHandleSelfUpdate_BypassesCheckSelfProtection` — fixture Server with `selfUpdater: fakeSpawner{...}` AND `selfService: "docker-update"`; POST /api/self-update (NOT /api/containers/docker-update/update); assert 202 (NOT 409) and verify the request did NOT route through CheckSelfProtection (assertable by ensuring no `self_protection` slog event was emitted in a captured handler)
      - `TestHandleUpdate_DockerUpdateSvc_StillReturns409` — control case: POST /api/containers/docker-update/update on the same fixture; assert 409 (the per-service endpoint still protects the self target — only /api/self-update bypasses)
      - `TestHandleSelfUpdate_409_ActionsInFlight` — fixture Server with `selfUpdater: fakeSpawner{...}` AND `actionsInFlightFn: func() int { return 1 }`; POST /api/self-update; assert 409 with body containing `actions_in_flight` (per RESEARCH.md Open Question 5)
      - Define a `fakeSpawner` test helper in the same file: `type fakeSpawner struct { returnID string; returnErr error }`; `func (f fakeSpawner) Spawn(ctx context.Context) (string, error) { return f.returnID, f.returnErr }` matching the `SelfUpdater` interface from RESEARCH.md Example 2

    **(C) `e2e/compose.test.yml` — append two services with `./test-relative-mount/<name>` volumes:**
      - Add a directory `e2e/test-relative-mount/` (create with `.gitkeep`) to the repo so the volume source path exists at compose-up time
      - Add services `relative-bind-A` and `relative-bind-B` to the `services:` block. Each uses image `zot:5000/centroid-is/stub:latest` (same pattern as the existing weston-stub fixture per project decision logged in STATE.md Phase 6 deviation: "weston-stub fixture image reused zot:5000/centroid-is/stub:latest"); labels `hmi-update.watch=true`, `hmi-update.allow-update=true`; volumes `- ./test-relative-mount/A:/data` (and `/B` for the second)
      - Each service has `restart: unless-stopped` and a `command:` that keeps it alive (e.g. `["sh","-c","sleep infinity"]` — match existing stub pattern in compose.test.yml)
      - Mirror the flutter/wayland-socket layout precisely: `./relative-path:/container-target` — this is the EXACT pattern that bit the operator per RESEARCH.md Pitfall 7

    **(D) `e2e/relative-bind-mount.spec.ts` (NEW FILE) — RED-first e2e spec for SC-2(b) + SC-6(iii):**
      - One Playwright test: `test('relative bind-mount resolves to operator host path after update', async ({ page, request }) => { ... })`
      - Steps:
        1. Hit `GET /api/state`; assert both `relative-bind-A` and `relative-bind-B` are present
        2. Inspect both via `docker inspect relative-bind-A` (use `child_process.execSync` per existing e2e pattern); record initial `HostConfig.Binds[0]` — assert it is an ABSOLUTE host path matching the expected pattern (e.g. `<host-cwd>/e2e/test-relative-mount/A:/data`)
        3. POST `/api/containers/relative-bind-A/update` (this triggers a no-op pull + recreate against the same image; the goal is to exercise the recreate path)
        4. Poll `/api/state` until the action completes (or timeout 30s)
        5. Re-inspect `relative-bind-A`; assert `HostConfig.Binds[0]` is STILL the same absolute host path (NOT mis-resolved to docker-update's container CWD)
        6. Repeat steps 3-5 for `relative-bind-B`
      - The test will FAIL on the current codebase because `internal/compose/runner.go` (the current Update path) shells out to `docker compose up -d --force-recreate` which mis-resolves `./relative-path` against the in-container compose path — exactly the BUG-7 / BUG-7b / BUG-7c / BUG-7d chain from HANDOFF.md
      - Add a top-of-file comment block: `// SC-6 (iii) RED-first regression guard for the relative-path bind-mount bug. Will pass after Plan 09-03 lands.`
      - Per existing project pattern (STATE.md Phase 6 weston-warning.spec.ts decision), this spec is RED on pre-Phase-9 and GREEN post-Phase-9 — commit shape is `test(09-02): ...` only

    Commits:
      - Commit 1: `test(09-02): add RED test TestUpdate_ComposeFileMoved_StillReturns412_PostSocketOnly (SC-6 i)`
      - Commit 2: `test(09-02): add RED handlers_self_test.go (SC-4 + SC-6 iv)`
      - Commit 3: `test(09-02): add e2e/compose.test.yml relative-bind-mount fixture (SC-2 b)`
      - Commit 4: `test(09-02): add RED e2e/relative-bind-mount.spec.ts (SC-2 b + SC-6 iii)`
  </behavior>
  <action>
    Implement all four sub-artifacts (A)-(D) as described in the `<behavior>` block.

    **(A) orchestrator_test.go append** — match the existing test fixture style (read `orchestrator_test.go` first to see the constructors). Use the package's existing fake-doubles where possible; introduce a new `fakeReader` only if `internal/actions/` does not already have one.

    **(B) handlers_self_test.go (NEW)** — `package api` (or `package api_test` if the file pattern in `handlers_actions_test.go` uses the `_test` variant). Use `httptest.NewRequest` + `httptest.NewRecorder`. Construct a Server via the existing `NewServer` constructor's exported test seam (or via direct struct literal if the package's test files do that today). For the `selfUpdater` field reference — the field does NOT EXIST on `Server` yet; that is intentional. The test file will fail to compile until Plan 09-04 adds `selfUpdater SelfUpdater` to the `Server` struct.

      Field-existence dependency: to make the RED state explicit and not just "compile error somewhere", structure the test to read `s.selfUpdater = nil` for the 503 case directly. If `Server` is unexported-field-only, fall back to constructing via an exported test helper (introduce `internal/api/testing.go` with a `NewServerForTest` function ONLY if necessary; prefer direct struct literal manipulation in the test file).

    **(C) compose.test.yml append** — minimal YAML diff appended to the `services:` block. Create the directory `e2e/test-relative-mount/A/` and `e2e/test-relative-mount/B/` (with `.gitkeep` in each) so the bind source paths exist. The fixture must satisfy the existing `make e2e-cron-fast` healthcheck wait (`docker compose up -d --wait`); use simple `sleep infinity` commands and no healthcheck (per STATE.md Phase 1 decision: distroless stubs use no healthcheck; host-side poll instead) — though the stub image here is the simple-busybox-style stub used in Phase 6, follow the same pattern: no healthcheck on the new services.

    **(D) relative-bind-mount.spec.ts (NEW)** — copy the imports and test-runner shape from an existing spec (e.g. `e2e/discovery.spec.ts` or similar). Use the existing API base URL pattern (`process.env.DOCKER_UPDATE_BASE || 'http://localhost:8080'`). The `child_process.execSync` call for `docker inspect` follows the existing project pattern (search `e2e/` for prior uses).

    All four artifacts land in separate commits as per the commit-shape list above. NO production code is touched in this plan — the RED commits intentionally fail to compile / fail to pass against pre-Phase-9 code.
  </action>
  <verify>
    <automated>
      cd /Users/jonb/Projects/tmp && go test ./internal/actions/... -run TestUpdate_ComposeFileMoved_StillReturns412_PostSocketOnly -race 2>&1 | tail -5 && (go test ./internal/api/... 2>&1 | grep -E 'undefined: .*[Ss]elfUpdater|cannot use nil|undefined: SelfUpdater' >/dev/null && echo "handlers_self RED confirmed") && grep -c "test\(" e2e/relative-bind-mount.spec.ts | awk '$1 >= 1 { exit 0 } { exit 1 }' && grep -c './test-relative-mount' e2e/compose.test.yml | awk '$1 >= 2 { exit 0 } { exit 1 }'
    </automated>
  </verify>
  <acceptance_criteria>
    - File `internal/actions/orchestrator_test.go` contains `func TestUpdate_ComposeFileMoved_StillReturns412_PostSocketOnly` (verified by `grep -c '^func TestUpdate_ComposeFileMoved_StillReturns412_PostSocketOnly' internal/actions/orchestrator_test.go` >= 1).
    - File `internal/api/handlers_self_test.go` exists and contains all five test functions (`TestHandleSelfUpdate_202_HelperSpawned`, `TestHandleSelfUpdate_503_WhenUnwired`, `TestHandleSelfUpdate_BypassesCheckSelfProtection`, `TestHandleUpdate_DockerUpdateSvc_StillReturns409`, `TestHandleSelfUpdate_409_ActionsInFlight`) — verified by `grep -c '^func Test' internal/api/handlers_self_test.go` >= 5.
    - File `internal/api/handlers_self_test.go` references type `fakeSpawner` (verified by `grep -c 'type fakeSpawner' internal/api/handlers_self_test.go` >= 1).
    - File `internal/api/handlers_self_test.go` references the route literal `POST /api/self-update` (verified by `grep -c 'POST /api/self-update' internal/api/handlers_self_test.go` >= 1).
    - `go test ./internal/api/...` exits non-zero with stderr containing one of: `undefined: SelfUpdater`, `s.selfUpdater undefined`, `handleSelfUpdate undefined`, or a mux-route 404 — this confirms RED.
    - File `e2e/compose.test.yml` contains exactly two `./test-relative-mount/` volume entries (verified by `grep -c './test-relative-mount' e2e/compose.test.yml` >= 2).
    - Directory `e2e/test-relative-mount/A/` and `e2e/test-relative-mount/B/` both exist and each contain a `.gitkeep` (verified by `ls e2e/test-relative-mount/A/.gitkeep e2e/test-relative-mount/B/.gitkeep`).
    - File `e2e/relative-bind-mount.spec.ts` exists and contains exactly one `test('relative bind-mount resolves to operator host path...'` call (verified by `grep -c "test\('relative bind-mount" e2e/relative-bind-mount.spec.ts` >= 1).
    - File `e2e/relative-bind-mount.spec.ts` contains the comment `// SC-6 (iii) RED-first regression guard` (verified by grep).
    - Git log shows four commits with prefixes `test(09-02):` (verified by `git log --format=%s --since='1 hour ago' | grep -c '^test(09-02):'` >= 4 — adjust window as needed for execution).
    - No file in `internal/` (production code, non-`_test.go`) was modified by this plan (verified by `git diff --name-only HEAD~4 HEAD -- 'internal/**/*.go' | grep -v '_test\.go$'` returning empty).
  </acceptance_criteria>
  <done>
    Four RED commits landed (one per sub-artifact). `go test ./internal/api/...` fails with `undefined: SelfUpdater` / `handleSelfUpdate undefined`. `e2e/relative-bind-mount.spec.ts` is RED against current main (would fail under playwright because the relative-path bug is still live). No production code touched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures → daemon API | E2e fixture introduces two new compose services bound to the test docker daemon; same trust posture as existing fixtures |
| Test fixture filesystem | `e2e/test-relative-mount/A,B/` are repo-tracked empty directories; no host secret exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Information Disclosure | `e2e/test-relative-mount/` host paths | accept | Paths are repo-relative; the only data is empty `.gitkeep` files; mirrors existing `e2e/` test fixture posture |
| T-09-02-02 | Tampering | `handlers_self_test.go` fakeSpawner | accept | Test-only; lives in `_test.go` files which production build excludes by Go test-file convention |
| T-09-02-03 | Denial of Service | New e2e fixture services consume daemon resources | accept | Two stub containers running `sleep infinity` on the test daemon; resource cost negligible; cleaned up by `docker compose down -v` in globalTeardown |
</threat_model>

<verification>
After all four RED commits:
- `go build ./internal/recreate/...` fails with "no Go files" (recreate package empty)
- `go test ./internal/api/...` fails with `undefined: SelfUpdater` or `s.selfUpdater undefined`
- `go test ./internal/actions/ -run TestUpdate_ComposeFileMoved_StillReturns412_PostSocketOnly` passes (this single test is the regression guard for compose.Reader's continued presence; documented as such in its comment block)
- `e2e/relative-bind-mount.spec.ts` would fail under `make e2e` because the relative-path bug is live in current main (verification deferred to Plan 09-03 GREEN)

Per C4: this plan ONLY lands RED commits. Plans 09-03 and 09-04 land the matching GREEN production code.
</verification>

<success_criteria>
- SC-6 (i): `TestUpdate_ComposeFileMoved_StillReturns412_PostSocketOnly` lives in `internal/actions/orchestrator_test.go` and asserts the behavior that must survive Plan 09-03's deletion of compose.Runner.
- SC-6 (ii): `TestRecreate_NoComposeProjectNameEnvDependency` lives in `internal/recreate/translate_test.go` and (once Plan 09-03 lands Translate) will assert pure-function behavior.
- SC-6 (iii): `e2e/relative-bind-mount.spec.ts` + `internal/recreate/translate_test.go::TestTranslate_HostConfig_Binds_AbsoluteAfterDaemonResolution` cover unit + e2e levels.
- SC-6 (iv): `internal/api/handlers_self_test.go` covers both the bypass (new endpoint) and the still-409 (existing per-service endpoint) behaviors.
- SC-2 (a) + (b): unit-level via translate_test.go HostConfig.Binds case; e2e-level via relative-bind-mount.spec.ts.
</success_criteria>

<output>
After completion, create `.planning/phases/09-architectural-hardening-post-v0-1-bug-cluster/09-02-SUMMARY.md` describing:
  - 4 RED commits landed with shapes
  - File list (5 files created/modified)
  - Verification: `go test ./internal/recreate/...` and `go test ./internal/api/...` both RED with the expected failure messages
  - Note for Plan 09-03 executor: "The RED commits in this plan unblock Plan 09-03's GREEN commits. Plan 09-03 MUST run the test suite after each implementation step and confirm tests flip from RED to GREEN one-by-one (per C4 RED→GREEN→REFACTOR)."
</output>
